[build]
  command = "npm run build"
  publish = "dist"

# Environment variables for production
[build.environment]
  VITE_API_URL = "https://jwt-api.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
